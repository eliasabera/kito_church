enum UserRole {
  student,
  teacher,
  admin,
}

enum RegistrationStatus {
  pending,
  approved,
  rejected,
}

enum AttendanceStatus {
  present,
  absent,
  late,
}

enum GiftStatus {
  pending,
  received,
  delivered,
}

enum GiftType {
  digital,
  physical,
}

enum ScoringCategory {
  attendance,
  quiz,
  assignment,
  bibleMemory,
  participation,
}
